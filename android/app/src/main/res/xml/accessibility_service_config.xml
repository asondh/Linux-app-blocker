<?xml version="1.0" encoding="utf-8"?>
<!-- Listens to content/state changes so it can read the address bar + search
     fields. packageNames is intentionally omitted so it can observe any browser;
     the service code filters to known browser packages. -->
<accessibility-service xmlns:android="http://schemas.android.com/apk/res/android"
    android:accessibilityEventTypes="typeWindowStateChanged|typeWindowContentChanged|typeViewTextChanged"
    android:accessibilityFeedbackType="feedbackGeneric"
    android:accessibilityFlags="flagReportViewIds|flagRetrieveInteractiveWindows"
    android:canRetrieveWindowContent="true"
    android:description="@string/accessibility_description"
    android:notificationTimeout="300" />
